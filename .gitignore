# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

#项目日志和上传资源目录
/logs/           # 项目运行时生成的日志目录
#/media/          # 通常用于存放用户上传的媒体资源（已被注释）

# 依赖目录与构建产物
node_modules     # Node.js 依赖包目录，在镜像中会通过 `npm install` 重新构建
dist             # 常见的项目构建输出目录
/dist
dist-ssr         # SSR (服务端渲染) 构建产物目录
dist.zip         # 构建产物的压缩包
package-lock.json # 锁定安装包版本的文件，镜像中依赖由 `package.json` 决定

# 编辑器目录和文件
.vscode/*        # VS Code 编辑器的工作区配置
!.vscode/extensions.json # 例外规则：不忽略此文件，允许其被复制（如果团队有统一插件要求）
.idea            # JetBrains IDE (如 WebStorm, PyCharm) 的项目配置目录
.DS_Store        # macOS 系统自动生成的文件夹属性文件
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?            # 各种编辑器产生的临时文件、项目文件等

#mac
.DS_Store        # 再次强调忽略 macOS 的 .DS_Store 文件

# virtualenv
/venv/           # Python 虚拟环境目录，镜像中应自行安装依赖
*.log
*.pot
*.py[cod]        # Python 编译生成的字节码文件 (.pyc, .pyo) 等
__pycache__      # Python 缓存字节码的目录
.env             # 环境变量文件，通常包含敏感信息，务必忽略！
.venv            # 另一种常见的虚拟环境目录名称